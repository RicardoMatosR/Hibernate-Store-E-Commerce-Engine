<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Configuración de la base de datos -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://127.0.0.1:3306/tienda_ces</property>
        <property name="hibernate.connection.username">root</property><!--root-->
        <property name="hibernate.connection.password">Richarlol04@!</property><!--root-->

        <!-- Dialecto de la base de datos -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Mostrar SQL en consola -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Estrategia de actualización del esquema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapeo de todas las clases -->
        <mapping class="model.User"/>
        <mapping class="model.Product"/>
        <mapping class="model.Profile"/>
    </session-factory>
</hibernate-configuration>
